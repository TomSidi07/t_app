export  function logo(links = [], classNames = ["link logo"]) {
  return `<a href="/" class="${classNames.join(" ")}">
  <span class="text text--white">t</span>
  a<span class="text text--green">p</span>p
</a>`;
}
